Create csv dump of the social activities table without having to fiddle with apparmor to allow INTO OUTFILE commands.
 
mysql -uUSERNAME -p -e "SELECT * from social_activity" prosolo | sed 's/\t/","/g;s/^/"/;s/$/"/' > ./social_activity.csv